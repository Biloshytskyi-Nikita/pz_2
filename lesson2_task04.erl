-module(lesson2_task04).
-export([len/1]).

len([]) -> 
    0; % Якщо список порожній, кількість елементів 0
len([_ | Tail]) -> 
    1 + len(Tail). % Додаємо 1 за кожен елемент і рекурсивно рахунемо довжину хвоста списку
